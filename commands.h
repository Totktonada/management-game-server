#ifndef COMMANDS_H_SENTRY
#define COMMANDS_H_SENTRY

#include "typedefs.h"
#include "main.h"

void execute_command(server_t *server, client_t *client, command_t *cmd);

#endif /* COMMANDS_H_SENTRY */
